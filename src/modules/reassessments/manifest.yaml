name: reassessments
displayName: Scheduled Reassessments
kind: job
enabled: true
consumes: []
produces: [drift]
scaleProfile:
  kind: job
  minReplicas: 0
  maxReplicas: 5
  triggers:
    - type: cron
      metadata: { schedule: "0 3 * * *" }
  cpu: 0.5
  memoryGi: 1.0
